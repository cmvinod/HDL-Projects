/**
 * Exclusive-or gate:
 * if ((a and Not(b)) or (Not(a) and b)) out = 1, else out = 0
 */
CHIP Xor {
    IN a, b;
    OUT out;

    PARTS:
    Nand(a= a, b= b, out= notab);
    Nand(a= a, b= notab, out= w1);
    Nand(a= notab, b= b, out= w2);
    Nand(a= w1, b= w2, out= out);
}